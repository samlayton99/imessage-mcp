Conversation: ${name} (${kind})
Identity on file: ${identity}
Previous monthly summary: ${monthly}

History (dated long-term archive, oldest first):
${history}

The last 30 days of messages — ${msg_count} message(s), oldest first:
${messages}
