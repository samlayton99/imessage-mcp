# watch.md — tag scratchpad (compiled into the active tag law)
#
# One tag per line as `- <slug>: <description>`. Slugs are lowercase and hyphenated.
# The summarizer may only tag conversations with the slugs listed here; anything else is dropped.
# Edit freely — add or remove lines. (The LLM curator that amends this on change is a later step;
# for now the file is read literally.)

- family: Immediate and extended family members.
- needs-scheduling: Threads where a specific time or date has recently been set.
- church: Church community, ministry, and related coordination.
